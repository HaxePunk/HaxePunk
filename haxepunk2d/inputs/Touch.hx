package haxepunk2d.inputs;

/**
 *
 */
class Touch
{
	/** If the user has a touch screen available. */
	public static var available(default, null) : Bool;

	/** */
	public static var multiTouchSupported(default, null) : Bool;
}
