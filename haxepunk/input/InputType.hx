package haxepunk.input;

/**
 * Represents a high level user action. InputTypes can be bound to one or
 * more concrete inputs, and can be used via polling or Signals.
 */
typedef InputType = String;
